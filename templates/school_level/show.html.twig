{% extends 'base.html.twig' %}

{% block title %}SchoolLevel{% endblock %}

{% block body %}
    <h1>SchoolLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ school_level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ school_level.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('school_level_index') }}">back to list</a>

    <a href="{{ path('school_level_edit', {'id': school_level.id}) }}">edit</a>

    {{ include('school_level/_delete_form.html.twig') }}
{% endblock %}
