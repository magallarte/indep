{% extends 'base.html.twig' %}

{% block title %}School{% endblock %}

{% block body %}
    <h1>School</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ school.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ school.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ school.address }}</td>
            </tr>
            <tr>
                <th>Zip_code</th>
                <td>{{ school.zipCode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ school.city }}</td>
            </tr>
            <tr>
                <th>Director_gender</th>
                <td>{{ school.directorGender }}</td>
            </tr>
            <tr>
                <th>Director_name</th>
                <td>{{ school.directorName }}</td>
            </tr>
            <tr>
                <th>Director_email</th>
                <td>{{ school.directorEmail }}</td>
            </tr>
            <tr>
                <th>Director_tel</th>
                <td>{{ school.directorTel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('school_index') }}">back to list</a>

    <a href="{{ path('school_edit', {'id': school.id}) }}">edit</a>

    {{ include('school/_delete_form.html.twig') }}
{% endblock %}
